import { UsuarioRouting.Module } from './usuario-routing.module';

describe('UsuarioRouting.Module', () => {
  it('should create an instance', () => {
    expect(new UsuarioRouting.Module()).toBeTruthy();
  });
});
